# Editor / assistant session-local config (per-machine, not for repo)
.claude/
CLAUDE.md

# Node
node_modules/
dist/
build/
.vite/
*.tsbuildinfo

# Env / secrets
.env
.env.*
!.env.example
*.pem
*.key
secrets.json

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
logs/
npm-debug.log*
pnpm-debug.log*

# Test / coverage
coverage/
.nyc_output/

# Even Realities build artifacts
*.ehpk

# Server runtime
server/data/
server/*.db
server/*.db-journal
server/dump.rdb

# pm2
.pm2/

# Backups
*.bak
*~

# Local scratch / screenshots with PII (per-machine, never commit)
STORE_LISTING.md
even hub screens/
qpp screens/

# Simulator/device captures. These render real contact names, phone numbers
# and email addresses straight onto the glasses frame, and the pre-commit PII
# guard greps text — it cannot see PII baked into a PNG. Keep them local.
screenshots/

# Local copy of Even Realities' own documentation (scraped for offline
# reference). Not ours to redistribute — read it at hub.evenrealities.com.
even-realities-docs.txt

# Session journals. Narrative build history kept locally — contains real
# contact details from testing that the PII guard would (correctly) block.
DEV/
