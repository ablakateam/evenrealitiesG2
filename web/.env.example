# VOX dashboard environment template.
# In production the dashboard is served same-origin with the API (Nginx),
# so VITE_API_BASE can be empty. For local dev, point it at the live server.

# VITE_API_BASE=https://your-server-domain.example
VITE_API_BASE=
